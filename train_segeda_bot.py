from agents.segeda_selector import SEGEDASelector

def main():
    # Inicializar el selector
    selector = SEGEDASelector()
    
    # El entrenamiento se realiza automáticamente en el constructor
    print("Entrenamiento completado. El bot está listo para usar.")

if __name__ == "__main__":
    main() 